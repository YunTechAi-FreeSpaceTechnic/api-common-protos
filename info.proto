
syntax = "proto3";

package predict;

message Empty {
}

message ModelInfo {
  string model_creator_name = 1;
  string version = 2;
}

message Labels {
  repeated string title = 1;
}

message AllModel {
  repeated ModelInfo models = 1;
}

service Info {
  rpc All(Empty) returns (AllModel);
  rpc Labels(Empty) returns (Labels);
}
